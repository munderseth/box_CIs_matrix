language: c

env:
  - JOB=ONE
  - JOB=TWO

build:
  ci:
   - mkdir -p $HOME/bin
   - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
   - testspace config url http://095d8acfaaf71b4a0f3767e41152620ebdda1b31@mark.stridespace.com
   - testspace -v
   - printenv > printenv.txt

  on_success:
   - |
    if [[ $JOB = "ONE" ]]; then
      testspace test.xml "+printenv.txt{environment variables}" "#/$JOB"
    else
      testspace test.xml "#/$JOB"
    fi
  on_failure:
   - |
    if [[ $JOB = "ONE" ]]; then
      testspace test.xml "+printenv.txt{environment variables}" "#/$JOB"
    else
      testspace test.xml "#/$JOB"
    fi